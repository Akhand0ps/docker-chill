docker run

```
Create and run a new container from an image

under the hood -> demon does its work

kiiling vs rm the container
>killing -> stops the container, data stays as it is
>rm-> deletes entirely
```



-it
```
open the particular container and attaches the terminal to the container's terminal

```